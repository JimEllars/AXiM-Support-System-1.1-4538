1. Verify all modified files (onyx-edge-worker/src/index.ts, src/components/tickets/OnyxInvestigationPanel.jsx, src/components/tickets/ActionProposalBlock.jsx) have the exact required modifications.
2. Ensure both `npm run build` and `npx tsc --noEmit` inside `onyx-edge-worker` pass with success.
3. Pre-commit check
