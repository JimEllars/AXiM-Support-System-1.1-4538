Successfully completed all tasks outlined in the ticket.

1. EmailDispatchManager Usage Consolidated: Replaced raw `fetch()` calls to Resend and EmailIt with `EmailDispatchManager` references across all outbound paths in `onyx-edge-worker/src/index.ts` (e.g., HITL dispatches, daily digests, leaderboard exports, automated AI replies).
2. Inbound Parsing & Threading: Updated the incoming webhook path `/api/v1/email/inbound` to securely verify the signature using `EmailDispatchManager.verifyEmailItSignature`.
3. Carrier Telemetry UI: Modified `src/components/tickets/MessageThread.jsx` to parse `msg.metadata.delivery_provider` and display a lightweight UI badge.
4. Health Telemetry: Added the `/api/onyx/email/health` endpoint to edge worker and bound it to `onyxService.fetchEmailCarrierHealth()`.
5. UI Resilience & Test Fixes (Sprint 1.2): Fixed `RCADocumentBlock.jsx` with strict nullish coalescing/optional chaining. Tests pass.
6. PublicIntake.jsx Hardening: Sanitized all text inputs, enforced 10MB payload size limits, and validated attachment extensions (.pdf, .png, .jpg, .txt, .log).
7. LiveChatPanel.jsx State Preservation: Preserved `currentDraft` in localStorage.
8. Edge Worker Hardening: Ensured `OPTIONS` requests return `204 No Content` with appropriate CORS headers in `onyx-edge-worker/src/index.ts`. Ensured `/health` endpoint reports the status of Edge KV, Supabase connectivity, and model configuration without throwing 500 errors.
9. Wired Live Telemetry: Connected `SupportMetrics.jsx` & `OperatorLeaderboard.jsx` directly to `ticket_ai_telemetry` and `team_profiles` via `supabaseClient.js`, replacing fallback arrays. Supported `is_curated` toggle.
10. UI/UX Modernization & Polish: Refined `CoreHealthIndicator.jsx` & `OnyxCommandHub.jsx` with clean Tailwind transitions, pulsating indicators, dark-mode typography, and standardized icon usage using `src/common/SafeIcon.jsx`.

Tests pass successfully (frontend tests via `npm test` and worker tests via `cd onyx-edge-worker && npm test`). The build succeeds on both sides.

[Session Log]
- Fixed RCADocumentBlock UI rendering with missing/partial data and improved markdown styling.
- Telemetry hardened: Swapped raw fetch blocking for `requestIdleCallback` dispatch batching.
- Worker CORS patched to explicitly handle all REST methods.
- UI Pill counters added to TicketList queue.
- Cleaned root repo architecture, moving utility scripts into `scripts/maintenance/`.
- Completed sprint 12 production hardening. All client and worker tests pass. Root `.cjs` maintenance scripts migrated to `scripts/maintenance/`.

# Increment Completed: Production Hardening & Telemetry Activation
*Date: 2026-09-15T18:23:25.533Z*

- **Milestone 1**: Telemetry pipeline activated. Wired src/lib/telemetry.js to the edge worker using `onyxService.recordTelemetry`. The edge worker is updated to support UI events and inserts to `ticket_ai_telemetry`.
- **Milestone 2**: HITL feedback loops wired. `ActionProposalBlock.jsx` and `AutoDraftWhisper.jsx` execute background fetch calls to `autodraft-feedback`, persisting to `memory_banks` and `product_feedback`.
- **Milestone 3**: DLQ hardening & Headers. Wrapped `handleBatchTriage`, `handleAutoDraft`, and `handleExecuteAction` in try/catch to buffer to `DLQ_BUCKET` if a timeout/key error occurs, returning `{ status: 'degraded', queued_to_dlq: true }`. Included security headers (`X-Onyx-Edge-Region`, `X-Onyx-Execution-Time-MS`). Added comprehensive `handleHealthCheck`.
- **Milestone 4**: UI/UX. `MessageThread.jsx`, `AutoDraftWhisper.jsx`, and `OnyxInvestigationPanel.jsx` import and apply `sanitizePayload`. `CoreHealthIndicator.jsx` throttled to 30s.

All tests (`npm test`) pass across root and `onyx-edge-worker`. Build executes successfully.

--- EmailIt API v2 with Resend Fallback (Phase Complete) ---
- Deployed EmailDispatchManager inside onyx-edge-worker with dual-provider architecture: EmailIt (Primary) and Resend (Fallback).
- Implemented 5-minute circuit breaker triggered by timeouts (>3500ms), quotas (HTTP 429), access issues (HTTP 403), or provider errors (HTTP 5xx).
- Telemetry extraction extracts `ratelimit-remaining`, `ratelimit-daily-remaining`, and `ratelimit-daily-reset` to proactively failover when daily quota is depleted.
- Completed webhook ingress parsing via POST /api/onyx/email/webhook with Cloudflare Web Crypto HMAC-SHA256 signature verification (checking against X-Emailit-Signature header).
- Frontend UI updated to display "EmailIt v2 Verified" vs "Resend Fallback Used" provenance pills.
- Added diagnostics polling to /api/email/telemetry to visualize system carrier state and rate limits inside CoreHealthDiagnosticsModal.jsx.
- Achieved 100% test pass rate across Vitest suites, including custom fallback mocking arrays.
- Bound new environment secrets: EMAILIT_API_KEY, EMAILIT_WEBHOOK_SECRET, RESEND_API_KEY. SPF combined mapping recorded to PROVISIONING_SECRETS.md.
